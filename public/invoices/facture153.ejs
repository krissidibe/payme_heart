<!DOCTYPE html>
<html lang="en">
<head>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

  <style>
    :root
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Inter:wght@400;600&display=swap');
  
  body {
    font-family: 'Poppins', sans-serif;;
  }
  .inter {
    font-family: 'Inter', sans-serif;
  }
  .poppin {
    font-family: 'Poppins', sans-serif;;
  }
  .page-header,
  .page-header-space {
    height: 100px;
  }
  
  .page-footer,
  .page-footer-space {
    height: 100px;
  }
  
  .page-footer {
    position: fixed;
    bottom: 0px;
    
    height: 80px;
    width: 100%;
    /* for demo */
    /* for demo */
  }
  
  .page-header {
    position: fixed;
    top: 0mm;
    width: 100%;
    border-bottom: 1px solid black; /* for demo */
    background: yellow; /* for demo */
  }
  
  .page {
    page-break-after: always;
    padding-bottom: 0px;
  }
  
  @page {
    margin: 0mm;
  }
  
  @media print {
    thead {
      display: table-header-group;
    }
    tfoot {
      display: table-footer-group;
    }
    
    button {
      display: none;
    }
    
    body {
      margin: 0;
    }
  }
  /* Facture Custom styling */
  
  /* End Facture Custom styling */
</style>

    <!-- Templates styling -->
  <style>
    .header {
    padding: 0px 55px;

    background: var(--primary-color, #06436B);
  }
    .bg-primary {
   

    background: var(--primary-color, #06436B);
  }
  .date {
    margin-top: 4px;
    padding-top: 8px;
    border-top: 1px solid #000100;
    font-size: 17px !important;
  }
    .headline-wrapper {
    width: auto;
    padding: 0 55px;
    padding-top: 10px;
    padding-bottom: 10px;
  }

  .facture-content .headline-wrapper .facture-number {
    display: none;
  }
    .sub-headline-wrapper {
    width: auto;
    margin: 0 55px;
  }
  .sub-headline {
    flex: 1;
  }
  .sub-headline > div {
    flex: 1;
  }

  .sub-headline > div {
   
    justify-content: space-between;
  }

  .hidden-subheadline {
    display: none;
  }

  .hidden-subheadline div {
    display: none;
  }

  .sub-headline p {
    width: auto;
    max-width: auto;
     padding: 0px 4px;
  }
 
  .facture-content .sub-headline-wrapper .facture-number {
    display: none;
  }
    .tabs-header {
    gap: 5px;
    width: auto;
    margin-left: 56px;
    margin-right: 56px;
    margin-bottom: 8px;
  }

  .tabs-header p {
    padding-top: 10px; /* /!\ */
    padding-bottom: 10px; /* /!\ */
  }
  
  .facture-content .tabs-header .description {
    padding-left: 10px;
  }

  .tabs-item {
    padding: 0 56px;
  }
  
  .tabs-item p {
    padding-top: 14px; /* /!\ */
    padding-bottom: 14px; /* /!\ */
    padding-left: 5px;
  }

  .tabs-item.empty-item {
    display: none;
  }
  
  /* Show default qty */
  .tabs .quantity {
    display: none;
  }
  
  .tabs .quantity.second {
    display: block;
  }
  
  /* Hide indexs */
  .tab-indexs {
    display: block;
  }

  .tabs .amount {
    padding-left: 0 !important;
  }
    .total {
    color: #4D4B4C;
    margin-top: 23.63px;
    padding-left: 56px;
    padding-right: 56px;
    align-items: flex-start;
    gap: 10px;
  }

  .left {
    max-width: 300px;
  }

  .right, .right > div {
    min-width: 260px;
    gap: 10px;
  }

  .right p {
    padding-left: 6px;
    padding-right: 14px;
  }
  
  .right p span {
    z-index: 2;
  }

  .right .remise {
    padding: 6px 12px 8px 6px;
    border: 1px solid #4D4B4C;
  }

  .right .total-amount {
    background: pink;
    padding: 10px 12px 10px 9px;
  }

  .total-separator {
    height: 1px;
    background: black;
    left: 0;
    right: 0;
  }
    .sign {
    margin-top: 8px;
    padding-top: 40px;
    padding-left: 110px;
    padding-right: 110px;
  }

  .sign > div p {
    width: 130px;
    padding-top: 0;
    padding-bottom: 8px; 
  }

  .sign > div:last-child {
     /* margin-top: -8px*/
     margin-top: 0;
  }
  /* <!-- addons styling --> */
    .tabs {
    padding-top: 0;
    padding-bottom: 0;
  }

  .tabs-header {
    border-top: 1px solid #4D4B4C;
    border-bottom: 1px solid #4D4B4C;
    gap: 0;
  }

  .tabs-header .description {
    padding-left: 22px !important;
  }

  .tabs-item .description {
    padding-left: 22px;
  }
  
  .tabs-item .amount {
    padding-right: 22px;
  }
      /* Pour ajouter du background au tabs items full-width*/
  .tabs-item:nth-child(even) {
    background: transparent;
  }
  
  /* Pour ajouter du background au tabs items not full-widht*/
  .tabs-item:nth-child(even) p {
    background: transparent;
  }
      .total {
    position: relative;
    padding-bottom: 70px;
  }
  
  .right .total-amount {
    background: rgb(199, 163, 163);
    position: absolute;
    bottom: 0;
    left: 56px;
    right: 56px;
    width: auto;
  }
    </style>
<!-- end addons styling -->
<style>
  /* custom styling */

  .header {
    flex-direction: row-reverse;
    padding-top: 24px;
    padding-bottom: 14px;
  }

  .header + div {
    background: var(--primary-color, #06436B);
  }
.header + div{
 
  margin: 0px !important;
  padding: 0px 56px !important;
  padding-top: 10px !important;
  padding-bottom: 10px !important;
}
  .header .logo img {
    object-position: right;
  
  }

  .header .facture-number p:first-child{
    font-size: 23px !important;
    margin-bottom: 4px;
  }

  .header .facture-number p {
    color: var(--primary-text-color, white);
  }

  .header .facture-number > div:first-child p {
    font-size: 35px;
  }

  .header .facture-number .date {
    border: none !important;
  }

  .sub-headline-wrapper {
    position: relative;
    color: var(--primary-text-color, white);
    padding-top: 24px;
    padding-bottom: 24px;
 
  }
 

  .tabs {
    margin-top: 30px;
  }

  .tabs-header {
    color: var(--primary-text-color, white);
    background: var(--primary-color, #06436B);
    border: none;
  }

  .tabs-header .amount {
    color: var(--secondary-text-color, #06436B);
    background: var(--secondary-color, #FCC10A);
  }

  .tabs-header + .tabs-item {
    margin-top: 30px;
  }

  .tabs-item:nth-child(even) p {
    background: #e9e9e9b1;
  }

  .tabs-item.last {
    border-bottom: 1px solid #4D4B4C;
  }

  .right .total-amount {
    padding: 0;
    color: var(--primary-text-color, white);
    background: var(--primary-color, #06436B) !important;
    
  }

  .right .total-amount span {
    padding: 10px;
  }


  .right .total-amount span:last-child {
    padding: 10px 18px;
    color: var(--secondary-text-color, #06436B);
    background: var(--secondary-color, #FCC10A);
  }

  .page-footer { 
  bottom: 0px;
  padding-top: 10px; 
  color: #000;
  /*font-family: Inter;*/
  font-style: normal;
  font-weight: 400;
  line-height: normal;
}

.gridView{
  display: grid !important;
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.gridView div:first-child {
  grid-column: span 2;
}
.facture-number div{
  display: flex;
   flex-direction: row;
   gap: 10px;
  align-items: center;
}

.facture-number p:last-child  { 
 padding-top: 0px !important;
  
}

.facture-number div p  {
  font-size: 23px !important;
  margin-bottom: 0px !important;
  
}

.facture-number div p:first-child  {
  font-size: 26px !important; 
  
  
}

.left{
  min-width: 400px;
}

  /* custom end styling */
</style>

<body>
  <!-- footer -->
  <div class="page-footer footer">
    <%- include('../subtemplates2b/footer1') %>
    <div class="absolute right-[-50px] bottom-[0px]">
      <svg xmlns="http://www.w3.org/2000/svg" width="204" height="39" viewBox="0 0 234 49" fill="none">
        <path d="M244.307 0.338867H0.68335V59.44H244.307V0.338867Z" fill="var(--primary-color, #06436B)"/>
      </svg>
    </div>
  </div>
  <table>
    <tbody>
      <tr>
        <td class="pages w-[827px]">
          <main class="<%= 'facture-content flex flex-col w-full page-content ' + (invoiceType == 0 ? 'proforma' : invoiceType == 1 ? 'facture' : invoiceType == 2 ? 'bordereau' : 'facture-content') %>">
            
            <!-- Header + Facture -->
      <div class="flex flex-col bg-primary">
        <%- include('../subtemplates2b/header/header1') %>
           
        <div class="px-10">
          <div class=" h-1 border-t-[1px] border-white   w-full"></div>  
        </div>
        <%- include('../subtemplates2b/headline/subheadline1') %>

        <div class="h-5"></div>
      </div>
            
            <!-- table -->
<%- include('../subtemplates2b/tabs/tab1') %>
            
                        <!-- facture totals informations -->
            <%- include('../subtemplates2b/total/total1') %>

            <!-- signature -->
            <%- include('../subtemplates2b/sign/sign1') %>
            
          </main>
        </td>
      </tr>
    </tbody>
    
    <tfoot>
      <tr>
        <td>
          <!--place holder for the fixed-position footer-->
          <div class="page-footer-space"></div>
        </td>
      </tr>
    </tfoot>
  </table>
</body>
</html>
