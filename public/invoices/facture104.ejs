<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
<script>
  
</script>
  <style>
    :root
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Inter:wght@400;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;;
    }
    .inter {
      font-family: 'Inter', sans-serif;
    }
    .poppin {
      font-family: 'Poppins', sans-serif;;
    }
    .page-header,
    .page-header-space {
      height: 100px;
    }

    .page-footer,
    .page-footer-space {
      height: 80px;
    }

    .page-footer {
      position: fixed;
      bottom: 80px;

      width: 100%;
      /* for demo */
      /* for demo */
    }

    .page-header {
      position: fixed;
      top: 0mm;
      width: 100%;
      border-bottom: 1px solid black; /* for demo */
      background: yellow; /* for demo */
    }

    .page {
      page-break-after: always;
      padding-bottom: 0px;
    }

    @page {
      margin: 0mm;
    }

    @media print {
      thead {
        display: table-header-group;
      }
      tfoot {
        display: table-footer-group;
      }

      button {
        display: none;
      }

      body {
        margin: 0;
      }
    }
    /* Facture Custom styling */
    .logo {
      background-color: #3E2256;
    }


    .primaryDiv{
      background: var(--primary-color,#6A1510);
      color: var(--primary-text-color,white); 
      
    }

    .primaryDiv-border{
      
      border-color: var(--primary-text-color,white); 
      
    }
.total .right {
  
  padding: 35px 0px 10px 0px !important ;
      background: var(--primary-color,#3E2256);
      color: var(--primary-text-color,white);
    }
.total .right p {
  padding: 5px 50px;
 
    }
    .total .remise {
  color: var(--primary-text-color,white) !important;
       border: none !important;
    }
    

    .tabs-item {
      color: #4D4B4C;
    }
    .tabs-item div {
     border: none !important;
    }

    .tabs .tabs-header p {
     background: var(--primary-color,#6A1510);
     color: var(--primary-text-color,white); 
     padding-top: 15px !important;
     padding-bottom: 15px !important;
    }
    
    .total .total-amount {
      padding-top: 15px  !important;
      border-top: 1px solid;
      border-color: var(--primary-text-color,white); 
      
      background:  var(--primary-color,#3E2256);
      color: var(--primary-text-color,white);
    }

    .footer {
      background: var(--primary-color,#3E2256);
      color:  var(--primary-text-color,white);
      bottom: 0;
    }

    .primaryText {
      color: var(--primary-color,#3E2256);
      
    }

  

    /* End Facture Custom styling */
  </style>
<!-- enterprise.name,
enterprise.address,
enterprise.rccm,
enterprise.email,
enterprise.bankNumber,
enterprise.numbers,
enterprise.numbers,
enterprise.numbers,
enterprise.numbers,
enterprise.nif, -->
  <body>
    <!-- footer -->
    <%- include('../subtemplates/footer1') %>
    <table>
      <tbody>
         

        <tr>
          <td class="pages w-[827px]">
             
           


            <div class="flex flex-col w-full page-content">
             
              <!-- logo + facture -->
              <header class="heading flex justify-between mt-[28.95px] px-14 pr-0 items-center">
 
                <div class="max-h-[120px] flex object-left object-contain h-[120px] max-w-[400px] w-[400px]  ">
                  <img class=" max-h-[120px] object-left object-contain h-[120px] max-w-[300px] w-[300px] " src="<%=logo%>" alt="" srcset="">
                  
                                </div>
                             
                                <div class="facture-number my-[15px] font-light text-[20px] primaryDiv inter max-w-[350px] w-[350px] pl-[70px] py-2  ">
                                  <div class="leading-[24px] mb-1">
                                    <% if(invoiceType == 0) { %>
                                      <p>PROFORMA</p>
                                    <% } %>
                                    <% if(invoiceType == 1) { %>
                                      <p>FACTURE</p>
                                    <% } %>
                                    <% if(invoiceType == 2) { %>
                                      <p  class="text-[18px]">BORDEREAU</p>
                                    <% } %> 
                                    <p >N°: <span><%=project.invoiceNumber.toString().padStart(5, '0')%></span> </p>
                                  </div>
              
                                  
                                  <% if(invoiceType == 0) { %>
                                  
                                    <p class="pt-2 mt-2  border-t-[1px] max-w-max font-light primaryDiv-border text-[17px] flex justify-between">Date: <%=date%></p>
                                  <% } %> 
                                  <% if(invoiceType == 1) { %>
                                  
                                    <p class="pt-2 mt-2  border-t-[1px] max-w-max font-light primaryDiv-border text-[14px] flex justify-between">Date: <%=date%></p>
                                  <% } %> 
                                  <% if(invoiceType == 2) { %>
                                  
                                    <p class="pt-2 mt-2  border-t-[1px] max-w-max font-light primaryDiv-border text-[19px] flex justify-between">Date: <%=date%></p>
                                  <% } %> 
              
                                </div>
              </header>
              
              
              
               
              
               
          
              <!-- headline (object, address, client) -->
              <%- include('../subtemplates/headline1') %>

              <!-- table -->
              <%- include('../subtemplates/table1Number') %>
                
              <!-- facture totals informations -->
              <%- include('../subtemplates/total1') %>


              <!-- signature -->
              <%- include('../subtemplates/sign1',signed,signature) %>

              <!-- footer -->
            </div>
          </td>
        </tr>
      </tbody>

      <tfoot>
        <tr>
          <td>
            <!--place holder for the fixed-position footer-->
            <div class="page-footer-space"></div>
          </td>
        </tr>
      </tfoot>
    </table>
  </body>
</html>









