<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <style>
    :root
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Inter:wght@400;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;;
    }
    .inter {
      font-family: 'Inter', sans-serif;
    }
    .poppin {
      font-family: 'Poppins', sans-serif;;
    }
    .page-header,
    .page-header-space {
      height: 100px;
    }

    .page-footer,
    .page-footer-space {
      height: 80px;
    }

    .page-footer {
      position: fixed;
      bottom: 80px;

      width: 100%;
      /* for demo */
      /* for demo */
    }

    .page-header {
      position: fixed;
      top: 0mm;
      width: 100%;
      border-bottom: 1px solid black; /* for demo */
      background: yellow; /* for demo */
    }

    .page {
      page-break-after: always;
      padding-bottom: 0px;
    }

    @page {
      margin: 0mm;
    }

    @media print {
      thead {
        display: table-header-group;
      }
      tfoot {
        display: table-footer-group;
      }

      button {
        display: none;
      }

      body {
        margin: 0;
      }
    }
    /* Facture Custom styling */
    .heading {
      background: #EBEBEB;
      margin-top: 94px;
      padding-left: 105px !important;
      padding-right: 105px !important;
    }

    .facture-number p:first-child {
      
      color: var(--primary-color,#44AC34);
    }

    .heading .logo {
      border-radius: 0 !important;
      background-color: #44AC34;
    }

  

    .pages .headline {
      margin-top: 15px;
      line-height: 18px;
      padding-top: 4px ;
      padding-bottom: 4px ;
   
    }
    .headline p {
      padding-left: 49px !important;
      font-size: 13.4px;
      font-weight: 400;
    }

    .pages .tabs {
      margin-top: 15px;
      padding-top: 55px;
      padding-bottom: 20px;
      background: #F5F5F5;
    }
    .tabs-item > div {
      
      border: none;
    }
   

    .tabs .separator {
      border: none;
    }

    .tabs-item {
      color: #4D4B4C;
    }

    .tabs .tabs-header {
      color: #242437;
      font-weight: 400;
    }

    .tabs .tabs-header p {
      background: #E8E7E7;
    }

    .tabs .tabs-header p:first-child {
      background: linear-gradient(90deg, var(--primary-color,#1A602B) 0%, var(--secondary-color,#28A238) 99.99%);
      color:  var(--primary-text-color,white);
    }

    .total .total-amount {
      background: linear-gradient(90deg, var(--primary-color,#1A602B) 0%, var(--secondary-color,#28A238) 99.99%);
      color:  var(--primary-text-color,white);
    }
    
    .footer {
      
   
      bottom: 0;
      background: linear-gradient(90deg, var(--primary-color,#1A602B) 0%, var(--secondary-color,#28A238) 99.99%);
      color:  var(--primary-text-color,white);
    }

    /* End Facture Custom styling */
  </style>

  <body>
    <!-- footer -->
    <%- include('../subtemplates/footer1') %>
    <table>
      <tbody>
        <tr>
          <td class="pages w-[827px]">

            <div class="flex flex-col w-full page-content">
              <!-- logo + facture -->
              <%- include('../subtemplates/header2') %>
          
              <!-- headline (object, address, client) -->
              <%- include('../subtemplates/headline1') %>

              <!-- table -->
              <%- include('../subtemplates/table1') %>
                
              <!-- facture totals informations -->
              <%- include('../subtemplates/total1') %>


              <!-- signature -->
              <%- include('../subtemplates/sign1') %>

              <!-- footer -->
            </div>
          </td>
        </tr>
      </tbody>

      <tfoot>
        <tr>
          <td>
            <!--place holder for the fixed-position footer-->
            <div class="page-footer-space"></div>
          </td>
        </tr>
      </tfoot>
    </table>
  </body>
</html>
