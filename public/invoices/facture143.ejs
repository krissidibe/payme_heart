<!DOCTYPE html>
<html lang="en">
<head>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

  <style>
    :root
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Inter:wght@400;600&display=swap');
  
  body {
    font-family: 'Poppins', sans-serif;;
  }
  .inter {
    font-family: 'Inter', sans-serif;
  }
  .poppin {
    font-family: 'Poppins', sans-serif;;
  }
  .page-header,
  .page-header-space {
    height: 100px;
  }
  
  .page-footer,
  .page-footer-space {
    height: 80px;
  }
  
  .page-footer {
    position: fixed;
    bottom: 0px;
    
    width: 100%;
    /* for demo */
    /* for demo */
  }
  
  .page-header {
    position: fixed;
    top: 0mm;
    width: 100%;
    border-bottom: 1px solid black; /* for demo */
    background: yellow; /* for demo */
  }
  
  .page {
    page-break-after: always;
    padding-bottom: 0px;
  }
  
  @page {
    margin: 0mm;
  }
  
  @media print {
    thead {
      display: table-header-group;
    }
    tfoot {
      display: table-footer-group;
    }
    
    button {
      display: none;
    }
    
    body {
      margin: 0;
    }
  }
  /* Facture Custom styling */
  
  /* End Facture Custom styling */
</style>

    <!-- Templates styling -->
  <style>
    .header {
    padding: 0px 55px;
  }
  .date {
    margin-top: 8px;
    padding-top: 8px;
    border-top: 1px solid #000100;
  }
    .headline-wrapper {
    width: auto;
    margin: 0 55px;
    padding-top: 10px;
    padding-bottom: 10px;
  }

  .facture-content .headline-wrapper .facture-number {
    display: none;
  }
    .sub-headline-wrapper {
    width: auto;
    margin: 0 55px;
  }
  .sub-headline {
    flex: 1;
  }
  .sub-headline > div {
    flex: 1;
  }

  .sub-headline > div {
    gap: 16px;
    justify-content: space-between;
  }

  .hidden-subheadline {
    display: none;
  }

  .hidden-subheadline div {
    display: none;
  }

  .sub-headline p {
    max-width: 150px;
  }
  .facture-content .sub-headline-wrapper .facture-number {
    display: none;
  }
    .tabs-header {
    gap: 5px;
    width: auto;
    margin-left: 56px;
    margin-right: 56px;
    margin-bottom: 8px;
  }

  .tabs-header p {
    padding-top: 10px; /* /!\ */
    padding-bottom: 10px; /* /!\ */
  }
  
  .facture-content .tabs-header .description {
    padding-left: 10px;
  }

  .tabs-item {
    padding: 0 56px;
  }
  
  .tabs-item p {
    padding-top: 14px; /* /!\ */
    padding-bottom: 14px; /* /!\ */
    padding-left: 5px;
  }

  .tabs-item.empty-item {
    display: none;
  }
  
  /* Show default qty */
  .tabs .quantity {
    display: none;
  }
  
  .tabs .quantity.second {
    display: block;
  }
  
  /* Hide indexs */
  .tab-indexs {
    display: block;
  }

  .tabs .amount {
    padding-left: 0 !important;
  }
    .total {
    color: #4D4B4C;
    margin-top: 23.63px;
    padding-left: 56px;
    padding-right: 56px;
    align-items: flex-start;
    gap: 10px;
  }

  .left {
    max-width: 300px;
  }

  .right, .right > div {
    min-width: 260px;
    gap: 10px;
  }

  .right p {
    padding-left: 6px;
    padding-right: 14px;
  }
  
  .right p span {
    z-index: 2;
  }

  .right .remise {
    padding: 6px 12px 8px 6px;
    border: 1px solid #4D4B4C;
  }

  .right .total-amount {
    background: pink;
    padding: 10px 12px 10px 9px;
  }

  .total-separator {
    height: 1px;
    background: black;
    left: 0;
    right: 0;
  }
    .sign {
    margin-top: 8px;
    padding-top: 40px;
    padding-left: 110px;
    padding-right: 110px;
  }

  .sign > div p {
    width: 130px;
    padding-top: 0;
    padding-bottom: 8px;
    border-bottom: 1px solid #4E4D4D;
  }

  .sign > div:last-child {
     /* margin-top: -8px*/
     margin-top: 0;
  }
  /* <!-- addons styling --> */
    .tabs {
    padding-top: 0;
    padding-bottom: 0;
  }

  .tabs-header {
    border-top: 1px solid #4D4B4C;
    border-bottom: 1px solid #4D4B4C;
    gap: 0;
  }

  .tabs-header .description {
    padding-left: 22px !important;
  }

  .tabs-item .description {
    padding-left: 22px;
  }
  
  .tabs-item .amount {
    padding-right: 22px;
  }
    .headline-subheadline-wrapper {
    flex-direction: row;
  }

  .sub-headline-wrapper {
    width: 50%;
  }

  .header {
    align-items: flex-end;
    border-left: 1px solid #4D4B4C;
    padding-left: 0;
  }


  .header .facture-number {
    padding-top: 30px;
  }

  .header .facture-number > div p {
    font-size: 32px;
    font-weight: 500;
  }

  .header .facture-number > div p:first-child {
    display: none;
  }

  .header .facture-number .date {
    border: none;
    margin-top: 0;
    padding-top: 4px;
    text-align: end;
  }
      /* Pour ajouter du background au tabs items full-width*/
  .tabs-item:nth-child(even) {
    background: transparent;
  }
  
  /* Pour ajouter du background au tabs items not full-widht*/
  .tabs-item:nth-child(even) p {
    background: transparent;
  }
      .total {
    position: relative;
    padding-bottom: 70px;
  }
  
  .right .total-amount {
    background: rgb(199, 163, 163);
    position: absolute;
    bottom: 0;
    left: 56px;
    right: 56px;
    width: auto;
  }
    </style>
<!-- end addons styling -->
<style>
  /* custom styling */

  .headline-subheadline-wrapper {
    flex-direction: row-reverse;
    padding: 70px 0 49px 0;
  }

  .header {
    align-items: flex-end;
    border-left: 1px solid #4D4B4C;
    padding-left: 0;
    margin-left: 0 !important;
  }

  .logo img {
    object-position: right;
  }

  .header .facture-number > div {
    display: flex !important;
  }
  
  .header .facture-number > div p {
    font-size: 24px !important;

  }

  .header .facture-number > div p:first-child {
    display: block;
    padding-right: 8px;
  }

  .header .facture-number .date {
    font-size: 16px !important;
  }

  .tabs-header {
    border: none;
    background: #EF7922;
    color: var(--primary-text-color, white);
    margin-bottom: 30px;
  }

  .tabs-header .description {
    background-color: var(--primary-color,#F9B233);
    color: var(--primary-text-color, white);
  }

 

  .tabs-header .quantity  {
    background-color: var(--secondary-color,#EF7922);
    color: var(--primary-text-color, white);
  }
  .tabs-header .price  {
    background-color: var(--secondary-color,#EF7922);
    color: var(--primary-text-color, white);
  }
  .tabs-header .amount  {
    background-color: var(--secondary-color,#EF7922);
    color: var(--primary-text-color, white);
  }

  .tabs-item:nth-child(even) p {
    background: #E9E9E9;
  }

  .tabs-item.last {
    padding-bottom: 16px;
  }

  .empty-item {
    border-top: 1px solid #4D4B4C;
    margin: 0 56px;
    padding: 0;
  }

  .total {
    padding-top: 24px;
  }

  .primary{
    color: var(--primary-text-color, white);
    background: linear-gradient(90deg, color-mix(in srgb, var(--primary-color,#F9B233) 100%, transparent) 51%,color-mix(in srgb, var(--secondary-color,#EF7922) 100%, transparent) 51.1% ,  color-mix(in srgb, var(--secondary-color,#EF7922) 100%, transparent)) !important;
 
  }

  .right .total-amount {
    color: var(--primary-text-color, white);
    background: linear-gradient(90deg, color-mix(in srgb, var(--primary-color,#F9B233) 100%, transparent) 51%,color-mix(in srgb, var(--secondary-color,#EF7922) 100%, transparent) 51.1% ,  color-mix(in srgb, var(--secondary-color,#EF7922) 100%, transparent)) !important;
  }

  .page-footer { 
    bottom: 0px;
    left: 0;
    right: 0;
    padding-bottom: 10px !important; 
    margin: 0 64px;
    border-top: 1px solid #4D4B4C;
    width: auto;
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* custom end styling */
</style>

<style>
  .logo {
    padding-left: 30px;
    width: 350px !important;
  }

  .headline-subheadline-wrapper {
    max-width: unset !important;
    padding-right: 30px;
    width: auto;
  }
  
  .sub-headline-wrapper {
    padding-top: 30px;
    width: 280px !important;
    flex: 1;
    margin-right: 0;
  }

  .sub-headline-wrapper p {
    font-size: 14px !important;
    max-width: unset !important;
    width: 250px !important;
  }
  
  .sub-headline > div {
    justify-content: flex-start;

  }

  .total {
    padding-top: 10px;
  }

  .left {
    max-width: 380px !important;
    padding-left: 22px;
  }

  .right {
    padding-right: 22px;
  }

  .total-amount {
    padding-left: 22px !important;
    padding-right: 22px !important;
  }

</style>

<body>
  <!-- footer -->
  <div class="page-footer footer">
    <%- include('../subtemplates2b/footer1') %>
  </div>
  <table>
    <tbody>
      <tr>
        <td class="pages w-[827px]">
          <main class="<%= 'facture-content flex flex-col w-full page-content ' + (invoiceType == 0 ? 'proforma' : invoiceType == 1 ? 'facture' : invoiceType == 2 ? 'bordereau' : 'facture-content') %>">
            <!-- Header + Facture -->
        <div class="pl-5">
          <%- include('../subtemplates2b/headerheadline/headerheadline3') %>
        </div>

            
            <!-- table -->
<%- include('../subtemplates2b/tabs/tab1') %>
            
                        <!-- facture totals informations -->
            <%- include('../subtemplates2b/total/total1') %>

            <!-- signature -->
            <%- include('../subtemplates2b/sign/sign1') %>
            
          </main>
        </td>
      </tr>
    </tbody>
    
    <tfoot>
      <tr>
        <td>
          <!--place holder for the fixed-position footer-->
          <div class="page-footer-space"></div>
        </td>
      </tr>
    </tfoot>
  </table>
</body>
</html>
